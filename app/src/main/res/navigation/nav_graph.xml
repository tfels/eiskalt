<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/InventoryListFragment">

    <fragment
        android:id="@+id/InventoryListFragment"
        android:name="de.felsernet.android.eiskalt.InventoryListFragment"
        android:label="@string/inventory_list_fragment_label"
        tools:layout="@layout/fragment_inventory_list">

        <action
            android:id="@+id/action_InventoryListFragment_to_SecondFragment"
            app:destination="@id/SecondFragment" />
    </fragment>
    <fragment
        android:id="@+id/SecondFragment"
        android:name="de.felsernet.android.eiskalt.SecondFragment"
        android:label="@string/second_fragment_label"
        tools:layout="@layout/fragment_second">

        <action
            android:id="@+id/action_SecondFragment_to_InventoryListFragment"
            app:destination="@id/InventoryListFragment" />
    </fragment>
</navigation>
